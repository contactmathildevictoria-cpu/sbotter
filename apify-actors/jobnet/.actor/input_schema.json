{
  "title": "Input — Sbotter Jobnet scraper",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "mode": {
      "title": "Run mode",
      "type": "string",
      "description": "fixture = push the bundled sample-items.json (for local testing). scrape = pull live ads from Jobnet.dk's BFF search API.",
      "editor": "select",
      "enum": [
        "fixture",
        "scrape"
      ],
      "default": "fixture"
    },
    "searchString": {
      "title": "Search string",
      "type": "string",
      "description": "Free-text query passed to Jobnet's search (e.g. 'marketing'). Empty = all ads.",
      "editor": "textfield",
      "default": ""
    },
    "maxItems": {
      "title": "Max items per run",
      "type": "integer",
      "description": "Soft cap so dev runs don't pull all ~23k ads.",
      "default": 100,
      "minimum": 1,
      "maximum": 5000
    },
    "orderType": {
      "title": "Sort order",
      "type": "string",
      "description": "Jobnet sort order for results.",
      "editor": "select",
      "enum": [
        "BestMatch",
        "PublicationDate"
      ],
      "default": "BestMatch"
    },
    "webhookUrl": {
      "title": "Webhook URL",
      "type": "string",
      "description": "Where to POST the scraped dataset. Overrides the SBOTTER_WEBHOOK_URL env var. Set by the scrape cron so the secret lives only in Vercel.",
      "editor": "textfield"
    },
    "webhookSecret": {
      "title": "Webhook secret",
      "type": "string",
      "description": "HMAC-SHA256 key for the x-sbotter-signature header. Overrides SBOTTER_WEBHOOK_SECRET.",
      "editor": "textfield",
      "isSecret": true
    }
  }
}
