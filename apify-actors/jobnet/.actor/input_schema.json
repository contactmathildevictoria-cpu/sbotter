{
  "title": "Input — Sbotter Jobnet scraper",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "mode": {
      "title": "Run mode",
      "type": "string",
      "description": "fixture = push the bundled sample-items.json (for local testing). scrape = crawl Jobnet.dk for real.",
      "editor": "select",
      "enum": ["fixture", "scrape"],
      "default": "fixture"
    },
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "Search result pages to crawl when mode = scrape.",
      "editor": "requestListSources",
      "default": [
        { "url": "https://job.jobnet.dk/CV/FindWork/Search?SortValue=BestMatch" }
      ]
    },
    "maxItems": {
      "title": "Max items per run",
      "type": "integer",
      "description": "Soft cap so dev runs don't crawl the whole site.",
      "default": 100,
      "minimum": 1,
      "maximum": 5000
    }
  }
}
